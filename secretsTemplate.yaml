
See the entries below for a template of how to define a secret.
use secrets in other yaml files with !secret <secret_name>

farm_wifi_ssid: "<which SSID to connect to>"
farm_wifi_pw: "<what the password is>"

fallback_ap_pw: "<what to use for fallback password>"